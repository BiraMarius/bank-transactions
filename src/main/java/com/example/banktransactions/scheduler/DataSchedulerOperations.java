package com.example.banktransactions.scheduler;

import org.springframework.stereotype.Component;

import java.time.LocalDate;

@Component
public class DataSchedulerOperations {
//CurrencyRates va fii salvat intr-un fisier, trebuie facuta scrierea in fisier si citirea din fisier a obiectului

    /*
    public long fullImport(){

    }

    public void checkAndImport(){
        LocalDate localDate = LocalDate.now();
        if(// daca localDate este egala cu data obiectului citit din fisier nu se face importul)
    }

     */
}
